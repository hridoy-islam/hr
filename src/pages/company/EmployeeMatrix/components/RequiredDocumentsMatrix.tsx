import { ComingSoonMatrix } from './ComingSoonMatrix';
import type { ModuleComponentProps } from '../types';

export const RequiredDocumentsMatrix = (props: ModuleComponentProps) => (
  <ComingSoonMatrix moduleLabel="Required Documents" {...props} />
);
