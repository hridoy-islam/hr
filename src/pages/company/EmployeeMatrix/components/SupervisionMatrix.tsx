import { ComingSoonMatrix } from './ComingSoonMatrix';
import type { ModuleComponentProps } from '../types';

export const SupervisionMatrix = (props: ModuleComponentProps) => (
  <ComingSoonMatrix moduleLabel="Supervision" {...props} />
);
