import { ComingSoonMatrix } from './ComingSoonMatrix';
import type { ModuleComponentProps } from '../types';

export const ImmigrationMatrix = (props: ModuleComponentProps) => (
  <ComingSoonMatrix moduleLabel="Immigration" {...props} />
);
