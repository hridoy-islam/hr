import { ComingSoonMatrix } from './ComingSoonMatrix';
import type { ModuleComponentProps } from '../types';

export const DisciplinaryMatrix = (props: ModuleComponentProps) => (
  <ComingSoonMatrix moduleLabel="Disciplinary" {...props} />
);
