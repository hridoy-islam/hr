import { ComingSoonMatrix } from './ComingSoonMatrix';
import type { ModuleComponentProps } from '../types';

export const InductionMatrix = (props: ModuleComponentProps) => (
  <ComingSoonMatrix moduleLabel="Induction" {...props} />
);
