import { ComingSoonMatrix } from './ComingSoonMatrix';
import type { ModuleComponentProps } from '../types';

export const PassportMatrix = (props: ModuleComponentProps) => (
  <ComingSoonMatrix moduleLabel="Passport Check" {...props} />
);
