import { ComingSoonMatrix } from './ComingSoonMatrix';
import type { ModuleComponentProps } from '../types';

export const DbsMatrix = (props: ModuleComponentProps) => (
  <ComingSoonMatrix moduleLabel="DBS Check" {...props} />
);
