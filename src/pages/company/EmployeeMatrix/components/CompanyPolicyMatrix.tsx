import { ComingSoonMatrix } from './ComingSoonMatrix';
import type { ModuleComponentProps } from '../types';

export const CompanyPolicyMatrix = (props: ModuleComponentProps) => (
  <ComingSoonMatrix moduleLabel="Company Policy" {...props} />
);
