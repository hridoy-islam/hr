import { ComingSoonMatrix } from './ComingSoonMatrix';
import type { ModuleComponentProps } from '../types';

export const QaMatrix = (props: ModuleComponentProps) => (
  <ComingSoonMatrix moduleLabel="Quality Assurance" {...props} />
);
