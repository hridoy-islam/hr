import { ComingSoonMatrix } from './ComingSoonMatrix';
import type { ModuleComponentProps } from '../types';

export const RtwMatrix = (props: ModuleComponentProps) => (
  <ComingSoonMatrix moduleLabel="Right to Work (RTW)" {...props} />
);
