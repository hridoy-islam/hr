import { ComingSoonMatrix } from './ComingSoonMatrix';
import type { ModuleComponentProps } from '../types';

export const SpotCheckMatrix = (props: ModuleComponentProps) => (
  <ComingSoonMatrix moduleLabel="Spot Checks" {...props} />
);
