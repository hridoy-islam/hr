import { ComingSoonMatrix } from './ComingSoonMatrix';
import type { ModuleComponentProps } from '../types';

export const HealthSafetyMatrix = (props: ModuleComponentProps) => (
  <ComingSoonMatrix moduleLabel="Health & Safety" {...props} />
);
